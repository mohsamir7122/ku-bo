# Improvement Cycle

```text
STATUS: ENGINEERING_CYCLE_COMPLETE; MODEL_PERFORMANCE_NOT_STARTED
MEASURED_IMPROVEMENT_PERCENT: NOT_AVAILABLE
```

Two engineering weaknesses were corrected: the stale task/branch control record,
and reflection of invalid raw row content in quarantine output. The final full
Stage 1 passed 2,273/2,273 tests versus the baseline's one control failure, and a new
negative test proves the quarantine does not reproduce a rejected credential
marker. Because the test denominator changed and no locked market set exists,
no percentage improvement is calculated.

Stage 2 adds 34 dossier/identity tests and the expanded suite passes
2,307/2,307. This is an engineering coverage increase, not a measured model or
investment-performance improvement.

Stage 3 adds explicit KCC coverage, seven access-executor tests, a TLS
compatibility regression test, and per-source exception isolation. The expanded
suite passes 2,316/2,316. Live probes correctly reported two blocked sources and
zero artifacts. This is a fail-safe engineering improvement; it is not a model
performance percentage.

Stage 4 removes two overlapping legacy cron activations and replaces them with
one locked seven-slot Kuwait schedule plus calendar, activation, ordering,
timeout, retry, and no-trade gates. Thirteen focused tests, seven slot dry runs,
Actionlint, and the 2,329-test suite pass. This increases schedule/recovery
prevention coverage only; controlled recovery metrics will be measured in the
next stage and no investment-performance percentage is inferred.

Stage 5 measured the recovery controls against the committed pre-upgrade state:

- critical-path fixed wait: 1,800 seconds → 0 seconds (100% removal);
- automatic workflow attempt cap: 3 → 2 (one fewer retry, 33.33% reduction);
- recomputed retry idempotency: absent → present and adversarially tested;
- immediate failed-jobs `workflow_run` path: absent → implemented/tested;
- missed-event-only watchdog: absent → implemented/tested at five-minute cadence;
- duplicate event, active-run, and exhausted-budget suppression: all controlled
  tests pass;
- six-hour alert deduplication and one validated CI-fix resume path: pass.

The recovery/schedule gate is 64/64 and the source/research evidence gate is
88/88. These are reproducible engineering-control deltas, not evidence of
forecast accuracy, market return, or investment improvement. Real observations,
training-admitted records, and locked predictions remain zero.

Stage 6 adds one fail-closed priority/checkpoint contract and measures only
coordination controls:

- exact priority mappings covered: 5/5;
- inclusive 90-day partition window covered: 90/90 dates;
- shard retry budget: bounded at 2, with third attempt rejected;
- completed-shard replay during controlled preemption: 0/2 completed shards;
- interrupted-shard controlled resume: 1/1, at attempt 2;
- stale generation/fencing, path traversal, symlink, and artifact hash attacks:
  rejected in adversarial tests;
- Champion byte mutation during the synthetic live-preemption scenario: 0;
- priority/checkpoint tests: 16/16; expanded recovery/lease tests: 35/35.

This is recovery coverage, not investment performance. Production durability
remains blocked, and source attempts, real observations, admitted unique events,
training records, and locked predictions remain unchanged at 2, 0, 0, 0, and 0.

Stage 7 adds an immutable rights-aware package contract and extends the existing
single scheduler without activating it:

- package unit/adversarial tests: 18/18;
- combined backfill, schedule, recovery, lease, priority, and workflow tests:
  106/106;
- production blocker Dry Run: exit 2 with no output created;
- real source attempts audited: 2; blocked: 2; readable artifacts: 0;
- planned date shards: 990; blocked before fetch: 180; unattempted: 810;
- real observations, unique events, research-context rows, training candidates,
  and contradictions: 0, 0, 0, 0, and 0;
- PyYAML parsing, Actionlint 1.7.12, compile, whitespace, and Secret Guard: pass.

These measurements improve audit/recovery coverage and prevent false completion
claims. They do not measure forecast quality or investment performance.

No model, feature, ranking, or risk improvement has been accepted. A later
improvement percentage may be reported only when `metrics-before.json` and
`metrics-after.json` are generated by the same reproducible locked test without
look-ahead, leakage, or overfitting.
