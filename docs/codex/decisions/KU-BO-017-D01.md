# KU-BO-017-D01 - Scope authority

```text
DECISION_ID: KU-BO-017-D01
STATUS: APPROVED
DATE: 2026-08-25
CATEGORY: SCOPE
MARKET_SCOPE: BOURSA_KUWAIT_ONLY
CANONICAL_ENGINE: kubo
PRIVATE_PREDECESSOR_MODE: READ_ONLY_SELECTIVE_REFERENCE
```

The user authorized a Kuwait-only operating scope and the safe reimplementation
of useful predecessor user jobs inside the canonical `kubo` package.

The authority permits:

- a locked single-market gate;
- sanitized capability parity;
- semantic source-fallback planning without network access;
- point-in-time portfolio and order validation;
- a disabled-by-default daily dry-run;
- one repository-local operating skill.

The authority does not permit importing repository history, publishing private
source details, running a second engine, bypassing access controls, enabling a
scheduler, training on unadmitted data, issuing a probability or recommendation,
connecting to a broker, submitting an order, or merging to a protected branch.

Publication is limited to a task branch and Draft PR after validation. The private
source location and revision details remain outside Git.
