# Deferred task - KU-BO-017 live dry-run orchestrator

```text
TASK_ID: KU-BO-017
STATUS: DEFERRED_BY_USER_PRIORITY
ORIGINAL_BASE_BRANCH: agent/ku-bo-016-codex-live-bootstrap
ORIGINAL_EXPECTED_BRANCH: agent/ku-bo-017-live-dry-run-orchestrator
MERGE_ALLOWED: NO
DEFERRED_IN_FAVOR_OF: KU-BO-MIG-001
```

This task was the active next task prepared by KU-BO-016. The user subsequently
made complete private-predecessor-to-KU-BO capability migration the active
priority. The task
is preserved here and in Git history; it is not deleted, merged, or authorized.

## Preserved mission

Turn the locked Codex handoff into a resumable private-data inventory and a
fail-closed daily dry-run orchestrator. Inspect `AI Rebuild` through an authorized
runtime connector, admit no Factor 9 artifact without its gates, and prove run
order and previous-session Champion-freeze controls without training a model,
collecting an unauthorized site, or issuing a stock recommendation.

## Preserved sequence

1. Verify remote/base/HEAD/PR/CI and run the bootstrap validator.
2. Discover `AI Rebuild` at runtime without persisting private identifiers.
3. Inventory KU-BO-relevant Drive candidates privately with hashes, provenance,
   rights, and Point-in-Time review state.
4. Write the private Factor 9 admission report without recrawling or recomputing
   the old score.
5. Implement a no-network, resumable, locked, no-overwrite daily dry-run.
6. Require the previous-session Champion freeze and reject same-day/future,
   Challenger, product/horizon, and hash mismatches.
7. Keep source probes inside authorized access recipes and treat them only as
   access evidence.
8. Keep scheduled shadow workflows disabled.
9. Prepare, but do not execute, later event-admission/trial-registry work.

## Preserved non-claims

- No live source promotion or scheduled activation.
- No private identifiers or bytes in Git.
- No model training, real backtest, forecast, probability, accuracy, buy/sell
  recommendation, entry/exit quote, automatic promotion, or financial execution.
- No merge without a later explicit user decision.

Before reactivating this task, reconcile it with everything implemented by
KU-BO-MIG-001, assign a new active task decision, and avoid duplicating migrated
pipeline, monitoring, Factor 9, or Skill work.
