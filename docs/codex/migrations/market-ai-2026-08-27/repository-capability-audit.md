# Kuwait-first repository capability audit — 2026-08-27

```text
RUN_ID: market-ai-20260827T020635Z-kuwait
STATUS: COMPLETED_FOR_DAY_ONE_SCOPE
PUBLIC_SUMMARY: SANITIZED
PRIVATE_RUNTIME_AUDIT_SHA256: 9f23680e65f60d3ffcea1d1c7ad6376aabd367b9cd46b3f68f47c0f4856836e5
PRIVATE_LOCATORS_OR_OIDS_PUBLISHED: NO
```

The private runtime audit binds every checkout to its exact repository, URL,
default branch, commit, tree, visibility, archive state, test result, and local
write policy. This public summary intentionally uses aliases for private sources.
The digest above permits later integrity verification without exposing locators.

## Verified canonical target

KU-BO is the correct Kuwait final target. Day one started from clean public
`main` at `93e4cab09915a4a4b58455d3cc45eb48be4bd499`; work is isolated on
`codex/kuwait-market-ai-day1-v1` with a local rollback branch at the exact base.

The canonical package already implements and tests:

- official-foundation, official-EOD, status/history, and corporate-action import
  and reconciliation contracts;
- allowlisted public capture with URL, redirect, DNS, robots, size, rate/failure,
  and immutable packet controls;
- effective-dated identity, market/session, evidence hashing, and provenance;
- event canonicalization, source-role boundaries, duplicate/context controls;
- causal split, purge/embargo, and temporal-leakage checks;
- research-only ranking, source quality/fallback, portfolio-state validation,
  champion freezes, and receipt-backed resumable dry runs;
- fail-closed `WATCH`/`ABSTAIN` boundaries and no financial execution.

These are software and synthetic/fixture capabilities. They do not prove a
reconciled Kuwait universe, real corpus, training, blind-test performance, or
live readiness.

## Verified predecessor and archive findings

The private predecessor is structurally valid and passed 937/937 tests plus its
secret scan. It has several user jobs that are not present as canonical KU-BO
modules:

1. governed SQLite evidence lifecycle with immutable source contracts,
   authorization windows, collection runs, raw artifacts, semantic receipts,
   issuer/press/social links, and schema-integrity checks;
2. deterministic multi-source reconciliation covering parser drift, blocked
   pages, point-in-time cutoff, revisions, independent-origin deduplication,
   critical-cell coverage, conflict quarantine, and fail-closed outputs;
3. a resumable per-company research planner/profile builder;
4. a guarded official historical pipeline and immutable remote receipt flow;
5. feature/label-separated training dataset and walk-forward lab builders;
6. a bounded momentum-event labeller and context-ledger components.

The private history source passed 48/48 tests from an isolated clone of its exact
HEAD. It adds long-horizon event provenance, access/rights auditing, cluster
deduplication, coverage ledgers, and preserved failure records. One direct
worktree run encountered a PRoot cross-filesystem `.git` pack copy error; this
was environmental and did not reproduce in the isolated clone.

The archived Kuwait implementation passed 33/33 tests. Its ranking, governance,
risk, learning, and report concepts substantially overlap or are superseded by
the canonical target and predecessor contracts, so it is audit-only and must not
be merged wholesale. The second archived Kuwait checkout contains only one
placeholder file and provides no code capability. The academic research source
is deferred because its literature-harvesting workflow is not an immediate
dependency of the Kuwait market runtime.

Saudi source and archive inventories were inspected read-only. The Saudi final
target is already integrated but has the same stale completed-task branch test
pattern (3/4 focused control tests pass). Saudi code changes remain prohibited
until Kuwait's validation and measurement gates pass.

## Gaps, contradictions, and disposition

- `config/predecessor_capability_parity.json` describes fourteen predecessor
  user jobs as bound or reimplemented, while the older migration preparation
  manifest and parity matrix still say discovery and all fourteen items are
  `NOT_STARTED`. Filesystem inspection confirms some listed user jobs exist only
  in the private predecessor. Therefore complete predecessor migration is not a
  supported claim yet.
- The target has no canonical per-company dossier contract satisfying the full
  master-contract fields and data-gap/source-coverage ledger.
- The target has no admitted real Kuwait company universe or historical event
  denominator; counts remain zero.
- There is no completed locked blind-test harness/report covering all requested
  metrics on real point-in-time data.
- Existing scheduled workflow coverage is limited and disabled; it does not
  implement the contractual Kuwait/Saudi local-time schedule.
- No required repository Secrets or variables are configured. Remote collection,
  Drive publication, model execution, and scheduled live scoring stay closed.

## Migration decisions

| Capability | Disposition | Reason |
| --- | --- | --- |
| Canonical KU-BO evidence/import/identity/actions/events/temporal core | KEEP | Tested target architecture; no second engine |
| Multi-source evidence lifecycle reconciliation | REIMPLEMENT_FIRST | Foundational gap for trustworthy collection and company dossiers |
| Governed evidence store | REFACTOR_INTO_KUBO | Valuable semantics, but direct 2,774-line tool copy would create a parallel architecture |
| Company daily planner/profile | REIMPLEMENT_AFTER_EVIDENCE | Depends on canonical evidence/admission contracts |
| Historical archive/access/dedup controls | REFACTOR_AFTER_RIGHTS_REVIEW | Useful, but raw historical bytes and private locators remain private |
| Training dataset/walk-forward builders | REIMPLEMENT_AFTER_EVENT_DENOMINATOR | Prevent leakage; cannot precede admitted data |
| Legacy Kuwait scoring/report engine | SUPERSEDE_WITH_SELECTIVE_TEST_IDEAS | Mostly overlapping; no wholesale merge |
| Empty archived Kuwait checkout | ARCHIVE_ONLY | No code capability found |
| Saudi engines and Factor archives | DEFER_TO_SAUDI_PHASE | Kuwait-first sequencing and missing admission tests |

No source repository was modified, merged, closed, archived, or deleted.
